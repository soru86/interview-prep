apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-js-demo.fullname" . }}
  labels:
    {{- include "nest-js-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nest-js-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
